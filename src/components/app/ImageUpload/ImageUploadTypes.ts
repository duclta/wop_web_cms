import { Ref } from "react";

export type ImageUploadProps = {
  refCustom?: any;
  defaultImage?: string;
  aspect?: number;
}

export type UseImageUploadProps = {
  refCustom?: any;
  defaultImage?: string;
}